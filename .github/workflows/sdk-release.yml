name: Zelta SDK Release

on:
  push:
    tags:
      - 'sdk-v*'

permissions:
  contents: read

jobs:
  publish:
    name: Notify Packagist
    runs-on: ubuntu-latest
    steps:
      - name: Notify Packagist
        env:
          PACKAGIST_USERNAME: ${{ secrets.PACKAGIST_USERNAME }}
          PACKAGIST_TOKEN: ${{ secrets.PACKAGIST_TOKEN }}
        run: |
          curl -fsS -X POST "https://packagist.org/api/update-package?username=${PACKAGIST_USERNAME}&apiToken=${PACKAGIST_TOKEN}" \
            -H "Content-Type: application/json" \
            -d '{"repository":{"url":"https://github.com/FinAegis/payment-sdk"}}'

      - name: Summary
        run: |
          VERSION="${GITHUB_REF#refs/tags/sdk-v}"
          echo "## Zelta SDK v${VERSION} Published" >> "$GITHUB_STEP_SUMMARY"
          echo "Install: \`composer require finaegis/payment-sdk\`" >> "$GITHUB_STEP_SUMMARY"
